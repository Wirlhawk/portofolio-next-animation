/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            "replit.com",
            "opinion-web.vercel.app",
            "dqpoykzl4wufkey0.public.blob.vercel-storage.com",
        ], // Add 'replit.com' to the list of allowed domains
    },
};

export default nextConfig;
